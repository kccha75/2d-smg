% function to locat
function [maxi,mini]=locatemaxmin(v)

[maxi,indexmax]=max(max(v)); % locate maximum, and index for max (for z)

mini=min(v1(200:300,indexmax)); % locate local minimum at slice v1(:,indexmax)
% 200-300 is picking somewhere in the middle ...


end